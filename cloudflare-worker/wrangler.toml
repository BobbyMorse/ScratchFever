name = "scratch-fever-scan"
main = "worker.js"
compatibility_date = "2024-09-23"

# Secrets — never stored here, set via CLI:
#   wrangler secret put SECRET_KEY          (same value as Railway SECRET_KEY)
#   wrangler secret put GOOGLE_GEMINI_API_KEY
